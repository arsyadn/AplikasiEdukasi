<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.presidentindonesia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PresidentIndonesia">
        <activity android:name=".KunciJawaban"></activity>
        <activity
            android:name=".IsiQuiz"
            android:label="Quiz"
            android:parentActivityName=".Quiz" />
        <activity
            android:name=".PenjelasanPresident"
            android:label="Detail"
            android:parentActivityName=".President" />
        <activity android:name=".splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HasilQuiz" />
        <activity android:name=".MainActivity" />
        <activity android:name=".About" />
        <activity android:name=".President" />
        <activity android:name=".Quiz" />

        <service android:name=".MyService"/>
    </application>

</manifest>